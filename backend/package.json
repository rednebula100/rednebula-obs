{
  "name": "rednebula-obs-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev":     "wrangler dev",
    "deploy":  "wrangler deploy",
    "db:init": "wrangler d1 execute rednebula-d1 --file=./src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
